"use strict";

/**
 * title-section-info service
 */

const { createCoreService } = require("@strapi/strapi").factories;

module.exports = createCoreService(
  "api::title-section-info.title-section-info"
);
